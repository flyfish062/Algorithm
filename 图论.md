
# 拓扑排序
## 思路
- 建立邻接表，初始化边的信息，构成图

```cpp
const int N = 510;//为点的最大数量
const int M = 1e5 + 10;//为边的最大数量
int e[M], ne[M], h[N], idx = 0;
//核心构建函数
void add(int a, int b){
    e[idx] = b;
    ne[idx] = h[a];
    h[a] = idx++;
}
```

- 构建一个队列（建议数组模拟，可以储存元素的拓扑排序顺序），遍历所有点，将入度为零节点加入队列；当队列不空时，重复取出队头元素，利用邻接表遍历其邻接点，将点都入度减1，判定最后此点入度，如果为0则加入队列。最后判定tt是否移动了n个单位（移动则说明所有点都遍历完成,输出一条拓扑排序顺序；否则输出不可能）。

```cpp
int d[N];//标记入度数量
int g[N], tt = -1, hh = 0;//模拟队列
void top_sort(){
    for(int i = 1; i <= n; i++){
        //节点编号从1到n
        if(d[i] == 0){
            g[++tt] = i;
        }
    }
    while(tt >= hh){
	    int temp = g[hh++];
        for(int i = h[temp]; i != -1; i = ne[i]){
            int j = e[i];
            d[j]--;
            if(d[j] == 0){
	            g[++tt] = j;
            }
        }
    }
    if(tt == n - 1){
        for(int i = 0; i < n; i++){
	        cout << g[i] << " ";
        }
    }else cout << "impossible";
}
```

- main函数，注意要初始化h`[]`。

```cpp
int n, m;
int a, b, c;
int main(){
    cin >> n >> m;
    memset(h, -1, sizeof h);
    while (m -- ){
        int a, b;
        cin >> a >> b;
        d[b]++;
        add(a, b);
    }
    top_sort();//进行拓扑排序
    return 0;
}
```

# 朴素Dijkstra

- 依旧是利用邻接表构建图

```cpp
#include <iostream>
#include <cstring>
const int N = 510;//为点的最大数量
const int M = 1e5 + 10;//为边的最大数量
int e[M], ne[M], h[N], idx = 0, w[M];
int dist[N];
int res;
int st[N];
//核心构建函数
void add(int a, int b, int c){
    e[idx] = b;
    ne[idx] = h[a];
    w[idx] = c;
    h[a] = idx++;
}
```

- 初始化所有点距离（dist`[]`）为`0x3f3f3f3f`(最大值)，将dist`[1]`初始化为0，
- 重复n次（节点次数）以下逻辑
- 遍历所有节点，找到dist最短的点，标记为已遍历（st`[i]` = true)(防止重复遍历);判断此节点距离是否为`0x3f3f3f3f`（是则表示节点为不联通，直接返回）；若正常，则遍历节点的邻接点(j)，更新dist（原来此邻接点距离原点的距离和通过标记节点的到达原节点的距离进行比较）。

```cpp
void Dijkstra(){
	memset(dist, 0x3f, sizeof dist);
	dist[1] = 0;
	for(int i = 0; i < n; i++){
		int t = -1;
		for(int j = 1; j <= n; j++){
			if(!st[j] && (t == -1 || dist[t] > dist[j]))
			t = j;
		}
		if(dist[t] == 0x3f3f3f3f){
			return;
		}
		st[t] = true;
		for(int i = h[t]; i != -1; i = ne[i]){
			int j = e[i];
			if(!st[j])
			dist[j] = min(dist[j], dist[t] + w[i]);
		}
	}
}
```

- main函数注意初始化h`[]`，最后判断要求的节点最短距离是否为无穷大（防止不联通点作用）

```cpp
int main()
{
    memset(h, -1, sizeof(h));

    cin >> n >> m;
    while (m--)
    {
        int a, b, w;
        cin >> a >> b >> w;
        add(a, b, w);
    }

    Dijkstra();
    if (dist[n] != 0x3f3f3f3f)
        cout << dist[n];
    else
        cout << "-1";
}
```


> 算法的主要耗时的步骤是从dist 数组中选出：没有确定最短路径的节点中距离源点最近的点 t。只是找个最小值而已，没有必要每次遍历一遍dist数组。于是想到可以使用小根堆进行优化。


# 堆优化的Dijkstra

- 利用小根堆的优势在于队头元素永远是最短dist的节点，但Dijkstra执行中的松弛操作在每一轮都会改变未标记点的dist的值，进而需要更新队列中的对应元素的dist值，**于是我们采用在更新dist后将更新后的{dist, 节点}加入队列，利用小根堆优先取出dist少的组合的特点，达到更新的效果（dist未更新的原组合会排在新组合的后面，而新组合弹出后会被打上标记（st = true），最后导致原组合不会被使用）**

## 程序

 ```cpp
 #include<iostream>
#include<queue>
#include<cstring>
#include<cstdio>
using namespace std;
const int N = 2e5 + 10;
int h[N], e[N], ne[N], w[N], idx = 0;
typedef pair<int, int> PII;
int dist[N];
bool st[N];
int n, m;
int a, b, c;

void add(int a, int b, int c){
  e[idx] = b;
  ne[idx] = h[a];
  w[idx] = c;
  h[a] = idx++;
}

void Dijkstra(){
    memset(dist, 0x3f, sizeof dist);
    dist[1] = 0;
    priority_queue<PII, vector<PII>, greater<PII>> heap;
    heap.push({0, 1});
    while(!heap.empty()){
        PII temp = heap.top();
        heap.pop();
        int dis = temp.first;
        int num = temp.second;
        if(st[num])continue;
        st[num] = true;
        for(int i = h[num]; i != -1; i = ne[i]){
            int j = e[i];
            if(dist[j] > dis + w[i]){
                dist[j] = dis + w[i];
                heap.push({dist[j], j});
            }
        }
    }
    if(dist[n] == 0x3f3f3f3f)cout << -1;
    else cout << dist[n];
}

int main(){
    cin >> n >> m;
    memset(h, -1, sizeof h);
    while(m--){
        cin >> a >> b >> c;
        add(a, b, c);
    }
    Dijkstra();
    return 0;
}
 ```
## 注意事项

- 为了实现按照dist大小排序，使用pair来储存<dist, `elemNum`>关键数据

```cpp
priority_queue<PII, vector<PII>, greater<PII>> heap; 
//这个数据结构只会对pair.first进行排序，dist要放在节点标号前面
``` 

- 更新dist后要将改变的<dist, `elemNum`>组合加入队列，来达到寻找最短dist组合的目的

```cpp
if(dist[j] > dis + w[i]){
    dist[j] = dis + w[i];
    heap.push({dist[j], j});
}
```

- 从朴素的遍历节点寻找最短dist 组合到从小根堆中取头节点，循环条件从一个O（n) 循环变成判断队列是否为空，**核心的变化就是要将更新的<dist, `elemNum`>加入heap中，直到heap中元素全部处理完为止**

# Bellman- Ford

> 一般用于解决有边数限制的含负权边/或者不含的最短路径问题
> 无边数限制的含负权边的最短路径问题：`SPFA`
> 无边数限制的不含负权边的最短路径问题： `Dijkstra`

**这里只记录解决有边数限制时的Bellman-Ford(即使用数组备份的算法)，不使用数组备份只适用于无有边数限制时的最短路径**

**使用 back 数组备份**
- 如果不使用 back 数组，在第i次迭代中更新dist`[y]` 时，可能会用到在当前第i次迭代中刚刚被更新过的 dist`[x]`。这会导致实际上只允许经过i条边，却因为“串联”更新，计算了经过大于i条边的路径。
- 使用 back 数组（即 back`[x]`）可以确保：计算经过最多i条边的最短路时，所有的转移都只能基于上一轮（最多经过 i−1条边）的计算结果。

**使用`dist[n] >= 0x3f3f3f3f / 2`判定孤立点**(Floyd,SPFA同理)
- `SPFA`和`Bellman_Ford`都可处理含负权边的最短路径，经过松弛后的不可达点（孤立点）也会被更新的小一些（负数作用）,但只是比`0x3f3f3f3f`小一些，于是用同位数的`0x3f3f3f3f / 2`进行比较。

## 思路
- 依旧是将每个节点的dist初始化为`0x3f3f3f3f`,然后将dist`[1]` = 0;
- 之后遍历每个边，比较dist`[b]`与back`[a]` + w`[i]`关系（松弛更新）
- 根据要求的边数限制重复执行（限制N条边，则重复N次上述逻辑）

**由于Ford算法只在乎边的松弛，无需邻接表/邻接矩阵记录邻接点（每个边中都包含了起始点，终点，权重三个信息），所以构建一个数据结构Edge记录三个要素即可**

- 记录每次加边的信息，构建Edge结构体

```cpp
#include<iostream>
#include<cstring>
using namespace std;
int n, m, k;
int a, b, c;
const int N = 510;//结点数
const int M = 1e5 + 10;//边数
struct Edge{
	int a;
	int b;
	int c;
}edge[M];
int back[N];
int dist[N];
```

 - 实现核心逻辑 **表示从 1 号点到 n 号点的最多经过 k 条边的最短距离**

```cpp
void Bellman_Ford(){
	memset(dist, 0x3f, sizeof dist);
	dist[1] = 0;
	for(int i = 0; i < k; i++)
	{
		memcpy(back, dist, sizeof dist);
		for(int i  = 0; i < m; i++){
		int x = edge[i].a;
		int y = edge[i].b;
		int w = edge[i].c;
		dist[y] = min(dist[y], back[x] + w);
		}
	}
	if(dist[n] >= 0x3f3f3f3f / 2)cout << "impossible";
	else cout << dist[n];
}
```

- main函数

```cpp
int main(){
    cin >> n >>m >> k;
    for(int i = 0; i < m; i++)
    {
        int a, b, c;
        cin >> a >> b >> c;
        edge[i] = {a, b, c};
    }
    Bellman_ford();
    return 0;
}
```

> 因为所有的edge都会被遍历松弛，min最后仍然会用重边中权值最小的一个edge。


# `SPFA`

Bellman-Ford算法原理是不断遍历每条边进行松弛，SPFA则是对其算法的优化，SPFA只对遍历边后dist更新的节点进行处理
- 将更新了dist数组的节点加入队列， 之后遍历其节点的邻接点，尝试更新邻接点的距离，更新成功则判断其是否入队，没有入队则入队，直到队列元素不存在。

## 思路
- 依旧构建邻接表,数组模拟队列。
```cpp
#include<iostream>
#include<cstdio>
#include<cstring>
using namespace std;
const int N = 1e5 + 10;//这题的节点和边数限制一样
int e[N], ne[N], h[N], w[N], idx = 0;
bool st[N];
int dist[N];
int g[N], tt = -1, hh =  0;
int n, m;
int a, b, c;
void add(int a, int b, int c){
	e[idx] = b;
	ne[idx] = h[a];
	w[idx] = c;
	h[a] = idx++;
}
```

- 按照上述思路构建核心函数

```cpp
void spfa(){
	memset(dist , 0x3f, sizeof dist);
	dist[1] = 0;
	st[1] = true;
	g[++tt] = 1;
	while(tt >= hh){
		int temp = g[hh++];
		st[temp] = false;
		for(int i = h[temp]; i != -1; i = ne[i]){
			int j = e[i];
			if(dist[j] > dist[temp] + w[i]){
				dist[j] = dist[temp] + w[i];
				if(!st[j]){
				g[++tt] = j;
				st[j] = true;
				}
			}
		}
	}
	if(dist[n] >= 0x3f3f3f3f / 2)cout << "impossible";
	else cout << dist[n];
}
```

- main函数

```cpp
int main(){
	memset(h, -1, sizeof h);
	cin >> n >> m;
	while(m--){
		cin >> a >> b >> c;
		add(a, b, c);
	}
	spfa();
	return 0;
}
```


# Floyd
**应对多源求最短路径问题时使用**

> 这种题目本身就带有一定的结构偏好性，显然，使用邻接矩阵能清晰表示不同节点的距离；而邻接链表用于高效查找某个节点的邻接点。邻接矩阵具有天然优势。
## 思路
- 遍历每个节点，用经过此节点的距离和原本的距离进行比较（松弛）

- 建立邻接矩阵(还体现在main函数中)
```cpp
#include<iostream>
#include<cstring>
using namespace std;
const int N = 510;
int n, m, k;//n为结点数，m为边数，k为询问次数
int a, b, c;
int x, y;//询问时的起始点和终点
int dist[N][N];
```

- 关键逻辑
```cpp
void floyd(){
	for(int k = 1; k <= n; k++){
		for(int j = 1; j <= n; j++){
			for(int i = 1; i <= n; i++){
				dist[i][j] = min(dist[i][j], dist[i][k] + dist[k][j]);
			}
		}
	}
}
```

- main函数
```cpp
int main(){
	//初始化邻接矩阵
	cin >> n >> m >> k;
	for(int i = 1; i <= n; i++){
		for(int j = 1; j <= n; j++){
			if(i == j)dist[i][j] = 0;
			else dist[i][j] = 0x3f3f3f3f;
		}
	}
	//添加数据
	while(m--){
		cin >> a >> b >> c;
		dist[a][b] = min(dist[a][b], c);//防止重边，这个是重点
	}
	floyd();
	while(k--){
		cin >> x >> y;
		if(dist[x][y] >= 0x3f3f3f3f / 2)cout << "impossible" << endl;
		else cout << dist[x][y] << endl;
	}
	return 0;
}
```

# 朴素Prim
> 不同于Dijkstra, Bellman-Ford, `SPFA`都是最短路径算法,`Prim`和`Kruskal`是求最小生成树(通过图的所有节点的权重之和最小的树)

**目标：给定一个 n 个点 m 条边的无向图，图中可能存在重边和自环，边权可能为负数。求最小生成树的树边权重之和，如果最小生成树不存在则输出 `impossible`**

- 无向图，链表容量要是 2 * M
- 边权有负数，所以最后用 `res >= 0x3f3f3f3f / 2`判定
## 思路
- 建立邻接链表（邻接矩阵也可以，这里演示邻接链表）

```cpp
#include<iostream>
#include<cstdio>
#include<cstring>
using namespace std;
const int N = 510;//节点最大个数
const int M = 1e5 + 10;
int e[2 * M], ne[2 * M], idx = 0, h[N], w[2 * M];//建立邻接链表
int n,m;
int a, b, c;
int dist[N];
bool st[N];
int res;
void add(int a, int b, int c){
	e[idx] = b;
	w[idx] = c;
	ne[idx] = h[a];
	h[a] = idx++;
}
```

- 初始化dist，将dist`[1]` = 0;遍历节点，找到dist最小的节点（离当前的最小生成树整体路径最近的节点），判断节点是否为不可达点（孤立点）,不为孤立点则st`[i]` =true(成为树的一部分，相当于被固定)，并执行 res += dist`[t]`;利用邻接表遍历此节点的邻接点并松弛（松弛方式与Dijkstra不同，更新dist）,反复执行N次（每次固定一个节点）。直到最后输出res。

```cpp
void Prim(){
	memset(dist, 0x3f, sizeof dist);
	dist[1] = 0;
	res = 0;
	for(int i = 0; i < n; i++){
		int t = -1;
		//遍历1-n节点编号数据，找dist最小节点
		for(int j = 1; j <= n; j++){
			if(!st[j] && (t== -1 || dist[t] > dist[j])){
				t = j;
			}
		}
		if(dist[t] == 0x3f3f3f3f){
			cout << "impossible";
			return;
		}
		st[t] = true;
		res += dist[t];
		for(int j = h[t];  j != -1; j = ne[j]){
			int temp = e[j];
			if(!st[temp] && dist[temp] > w[j]){
				dist[temp] = w[j];
			}
		}
	}
	if(res >= 0x3f3f3f3f / 2)cout << "impossible";
	else cout << res;
}
```

- main函数

```cpp
int main(){
	cin >> n >> m;
	memset(h, -1, sizeof h);
	while(m--){
		cin >> a >> b >> c;
		add(a, b, c);
		add(b, a, c);//无向图
	}
	Prim();
	return 0;
}
```


**Prim和Dijkstra的思路极其的像，但有以下几点不同点**

Prim：dist为节点到生成的树这个整体的最小距离
Dijkstra：dist为节点到原点的最小距离

Prim：更新策略
```cpp
for(int j = h[t];  j != -1; j = ne[j]){
			int temp = e[j];
			if(!st[temp] && dist[temp] > w[j]){
				dist[temp] = w[j];
			}
		}
	}
```



Dijkstra：更新策略

```cpp
for(int i = h[t]; i != -1; i = ne[i]){
			int j = e[i];
			if(!st[j])
			dist[j] = min(dist[j], dist[t] + w[i]);
		}
	}
```



负权边处理：

|              |                 |                                     |              |                                   |
| ------------ | --------------- | ----------------------------------- | ------------ | --------------------------------- |
| 算法           | dist[i] 的含义     | 更新公式                                | 依赖的前提        | 负权边的影响                            |
| **Dijkstra** | 到起点的**路径累加和**   | dist[y] = min(dist[y], dist[x] + w) | 路径单调递增（无负权边） | **失效**。负权边破坏了单调性，导致提前锁死的点结果错误。    |
| **Prim**     | 到生成树的**最短单边距离** | dist[y] = min(dist[y], w)           | 割性质（对正负无要求）  | **有效**。负权边仅代表一条非常廉价的边，不影响割性质的正确性。 |

- **重边**：邻接表中会存入多条重边，但在松弛操作 dist`[temp]` > w`[j]` 时，由于只取较小值，算法会自动保留最短的那条边。
    
- **自环**：自环的终点就是起点自身。在更新邻接点时，由于起点已被标记为 st`[t]` = true，判定条件 !st`[temp]` 会直接过滤掉自环，因此自环不会影响结果。


# `Kruskal`


### 目标
给定一个 n 个点 m 条边的**无向图**，图中可能存在重边和自环，边权可能为负数。
求最小生成树的树边权重之和，如果最小生成树不存在则输出 `impossible`。
1≤n≤1e5,  
1≤m≤2∗1e5,  
图中涉及边的边权的绝对值均不超过 1000。

## 思路
- 和Bellman-Ford思路相似。通过遍历边来更新，得到最小生成树

- 记录每次的边数据，按升序顺序进行排列，依次尝试当前最小的边更新图结构。
- 当边的起始点和目标点不在同一个集合里（对点使用并查集），则用边数据更新res += edge`[i]`.w，并将2个点连通（成为相同集合）;如果2个点在同个集合则跳过（不做操作），直到最后边全部用完。
- 用变量（`cnt = 0`)记录每次更新，如果变量(`cnt <= n - 1`)说明出现不联通点，不是联通图


- 和Bellman-Ford一样，只注意选取边然后更新，只用构建一个数据结构来储存边信息即可
- 利用了并查集，要构建并查集基本函数

```cpp
#include<iostream>
#include<cstring>
#include<algorithm>
const int  N = 1e5 + 10;
int p[N];//并查集
using namespace std;
int n, m;
int a, b, c;
int res;
int cnt;
struct Edge{
	int a;
	int b;
	int w;
	bool operator < (const Edge& num){
		return this -> w < num.w; 
	}//定义排序方法
}edge[2 * N]

int find(int a){
	if(p[a] != a) p[a] = find(p[a]);
	return p[a];
}
```

- 核心逻辑

```cpp
void Kruskal(){
	res = 0;
	cnt = 0;
		//注意这里是m,是在遍历边的个数，边个数由m确定
	for(int i = 0; i < m; i++){
		int pa = find(edge[i].a);
		int pb = find(edge[i].b);
		if(pa != pb){
			res += edge[i].w;
			p[pa] = p[pb];
			cnt++;
		}
	}
	if(cnt == n - 1)cout << res;
	else cout << "impossible";
}
```

- main函数

```cpp
int main(){
	cin >> n >>m;
	for(int i = 1; i <= n; i++)p[i] = i;//并查集初始化
	int temp = m;//之后要用m这个变量进行排序，不能被损失
	while(temp--){
		cin >> a >> b >> c;
		Edge edge[temp] = {a, b, c};
	}
	sort(edge, edge + m);//注意要和上面的for(int i = 0; i < m; i++)匹配
	Kruskal();
	return 0;
}
```

> 不用关心无向图，因为`Kruska`从边的角度进行优化更新，并不是和Prim那样通过寻找最短dist访问邻接点来进行松弛，边的关系一旦联通就是自带双向性。


# 染色法判定二分图


## 目标

**给定一个 n 个点 m 条边的无向图，图中可能存在重边和自环。
请你判断这个图是否是二分图。**

## 思路
- 依旧是利用邻接表构建图，建立节点之间的关系
- 利用递归思路，构建一个递归函数，将当前节点染色，然后利用当前节点遍历邻接点，判定邻接点是否被染色（没被染色则对此节点进行函数递归（继续为这个节点染色，并查看其邻接点情况）（被染色则看其颜色是否与原节点不同，不同则返回否，表明情况不符合，返回上一个递归函数），在最后写 return true；如果全都没有问题，则返回true ,说明这个图为2分图。
```cpp
#include<iostream>
#include<cstdio>
#include<cstring>
using namespace std;
const int N = 1e6 + 10;
int e[2 * N], ne[2 * N], h[N],idx = 0;
int color[N];
//0表示无颜色，1表示颜色1，2表示颜色2;
int n, m;
int a, b;

void add(int a, int b){
    e[idx] = b;
    ne[idx] = h[a];
    h[a] =idx++;
}
bool dfs(int u, int y){
    //u为被染色节点，i表示不同颜色
    color[u] = y;
    for(int i = h[u]; i != -1; i = ne[i]){
        int j = e[i];
        if(!color[j]){
           if(!dfs(j, 3- y))return false; 
        }else
        {
            if(color[j]&& color[j] != 3 - y)
            return false;
        }
    }
    return true;
}

int main(){
    cin >> n >> m;
    memset(h, -1, sizeof h);
    while(m--){
        cin >> a >> b;
        add(a, b);
        add(b, a);
    }
    //防止非联通图
    for(int i = 1; i <= n; i++)//遍历点
    {
        if(!color[i])//如果没染色
        {
            if(!dfs(i, 1))//染色该点，并递归处理和它相邻的点
            {
                cout << "No" << endl;//出现矛盾，输出NO 
                return 0;
            }

        }
    }
    cout << "Yes" << endl;//全部染色完成，没有矛盾，输出YES
    return 0;
}
```

> 防止非联通图，所以在main函数里要遍历所有节点，尝试对所有节点进行染色并判断。

> 无向图，开邻接表时数组大小为节点最大数的2倍

> 在递归染色过程中，记住递归函数返回结果的传递
```cpp
if(!color[j]){
           if(!dfs(j, 3- y))return false; 
        }else
        {
            if(color[j]&& color[j] != 3 - y)
            return false;
            //递归最后就是在这个条件下返回对情况的判定（true还是false)
        }
```

**递归处理，并接受处理后给出的结果，对邻接节点进行染色递归，接收如果对其染色造成的结果，如果错误则也要返回错误（构建递归传递信息的递归链）**


# 匈牙利算法

> 用增广路径求二分图最大匹配的算法。

## 目标

给定一个二分图，其中左半部包含 n1 个点（编号 1∼n1），右半部包含 n2 个点（编号 1∼n2），二分图共包含 m 条边。

数据保证任意一条边的两个端点都不可能在同一部分中。

请你求出二分图的最大匹配数

#### 输入格式

第一行包含三个整数 n、 m 和 k。

接下来 k 行，每行包含两个整数 u 和 v，表示左半部点集中的点 u 和右半部点集中的点 v 之间存在一条边。

#### 输出格式

输出一个整数，表示二分图的最大匹配数。



## 思路

- 依旧用邻接表构建图
- 构建find递归函数，对当前匹配节点遍历其邻接节点（要匹配的节点：另一边的节点），判断是否被标记（标记则跳过，继续寻找下一个匹配节点：为了在多次递归中实现固定节点，防止原先处理过的节点在后续递归中再次被重新匹配处理），如果没被标记，则查看其点（遍历取出的匹配点是否为0，或者已经被匹配但通过 对这个匹配点相匹配的点 进行find递归函数处理可以返回true）,则表明可以将这个匹配点和当前节点相匹配，完成当前的二分匹配，返回true。但如果遍历完所有节点后仍不能返回true，则说明不能实现二分最大匹配，返回false。

## 实现-核心代码

```cpp
#include<iostream>
#include<cstdio>
#include<cstring>
using namespace std;
const int N = 510;
const int M = 1e5 + 10;
int e[M], ne[M], h[N], idx = 0;
int n, m ,k;
int a, b;
int st[N];
int match[N];

void add(int a, int b){
	e[idx] = b;
	ne[idx] = h[a];
	h[a] = idx++;
}

bool find(int u){
	for(int i = h[u]; i != -1; i = ne[i]){
		int j = e[i];
		if(!st[j]){
			st[j] = 1;
			if(match[j] == 0 || find(match[j]))
			{
				match[j] = u;
				return true;
			}
		}
	}
	return false;
}

int main(){
	memset(h, -1, sizeof h);
	cin >> n >> m >> k;
	while(k--){
		cin >> a >> b;
		add(a, b);
	}
	int res = 0;
	for(int i = 1; i <= n; i++){
		memset(st, 0, sizeof st)
		if(find(i))res++;
	}
	cout << res << endl;
	return 0;
}
```

> 细节在于每次匹配后要重置st数组，防止出现干扰。
>```
for(int i = 1; i <= n; i++){
		memset(st, 0, sizeof st)
		if(find(i))res++;
	}
> ```
